name: Cron Job Validation
on:
  pull_request:
  push:

jobs:
  validation:
    name: Suggest Chain Validation Check
    runs-on: ubuntu-20.04

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup nodejs
        uses: actions/setup-node@v3
        with:
          node-version: "16"

      - run: npm install
      - run: npm install --global yarn

      - name: Fetch chain files
        id: fetch
        uses: Rishabh510/Path-lister-action@master
        with:
          path: "cosmos/"
          type: ".json"

      - name: Check chain updates
        id: check-update
        continue-on-error: true
        run: |
          LINT=$(yarn ts-node src/schedule.ts)
          echo "$LINT"

      # - name: Slack Alert Notification
      #   uses: slackapi/slack-github-action@v1.23.0
      #   with:
      #     channel-id: ${{ secrets.SLACK_CHANNEL_ID }}
      #     payload: |
      #       {
      #         "text": "Deployment started (In Progress)",
      #         "attachments": [
      #           {
      #             "color": "dbab09",
      #             "fields": [
      #               {
      #                 "title": "Status",
      #                 "short": true,
      #                 "value": "output value ${{ steps.check-update.outputs.errorOutput }}"
      #               }
      #             ]
      #           }
      #         ]
      #       }
      #   env:
      #     SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
